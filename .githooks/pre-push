#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

echo "[pre-push] running full test suite (go test ./... -count=1)..."
go test ./... -count=1

echo "[pre-push] OK"
